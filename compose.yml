services:
  openapi:
    image: ghcr.io/nuoxiantech/openapi:latest
    pull_policy: always
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - openapi-data:/app/.data

volumes:
  openapi-data:
