services:
  openapi-platform:
    image: ghcr.io/nuoxiantech/openapi-platform:latest
    restart: unless-stopped
    environment:
      NUXT_AUTH_SECRET: "${NUXT_AUTH_SECRET:?NUXT_AUTH_SECRET must be set}"
      NUXT_API_KEY_SECRET: "${NUXT_API_KEY_SECRET:?NUXT_API_KEY_SECRET must be set}"
      DATABASE_URL: "${DATABASE_URL:-}"
      NUXT_REDIS_URL: "${NUXT_REDIS_URL:-}"
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - app_data:/app/.data
    healthcheck:
      # /api/health is a liveness probe; readiness must include initialization,
      # database and required Redis dependencies before traffic is sent here.
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/api/ready').then(response => { if (!response.ok) process.exit(1) }).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - openapi

volumes:
  app_data:
    name: openapi-app-data

networks:
  openapi:
    external: true
    name: openapi-network
